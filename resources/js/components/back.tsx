"use client";

import { Button } from "@nextui-org/react";

export default function BackButton() {
    return (
        <Button variant="bordered" size="sm" onClick={() => {}}>
            Go Back
        </Button>
    );
}
